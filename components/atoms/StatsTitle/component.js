import styled from 'styled-components';

const StyledStatsTitle = styled.p`
  color: ${props => props.theme.colors.blue500};
  line-height: 20px;
  margin-top: 10px;
`;

export default StyledStatsTitle;
