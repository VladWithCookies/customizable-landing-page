import styled from 'styled-components';

const GalleryImageCategory = styled.p`
  margin-top: 10px;
  font-weight: 300;
  font-size: 12px;
  line-height: 16px;
  letter-spacing: 1px;
`;

export default GalleryImageCategory;
